[package]
name = "surfacecheck-service"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Bounded authenticated runtime IPC and single-flight operation state"

[dependencies]
libc.workspace = true
serde.workspace = true
serde_json.workspace = true
surfacecheck-capture = { path = "../surfacecheck-capture" }
surfacecheck-core = { path = "../surfacecheck-core" }
surfacecheck-review = { path = "../surfacecheck-review" }
surfacecheck-store = { path = "../surfacecheck-store" }
