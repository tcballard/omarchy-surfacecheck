[package]
name = "surfacecheck-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Strict schemaVersion 1 JSON command surface for SurfaceCheck"

[dependencies]
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
surfacecheck-capture = { path = "../surfacecheck-capture" }
surfacecheck-core = { path = "../surfacecheck-core" }
surfacecheck-service = { path = "../surfacecheck-service" }
